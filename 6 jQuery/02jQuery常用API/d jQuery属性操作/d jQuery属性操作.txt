jQuery 属性操作

1  设置或获取元素固有属性值 prop()  01
    所谓元素固有属性就是元素本身自带的属性，比如 <a> 元素里面的 href ，比如 <input> 元素里面的 type。 
    a. 获取属性语法
        prop('属性')

    b. 设置属性语法
        prop('属性', '属性值')



2  设置或获取元素自定义属性值 attr()    01
    用户自己给元素添加的属性，我们称为自定义属性。 比如给 div 添加 index =“1”。 

    a. 获取属性语法
        attr('属性')            //类似原生 getAttribute()

    b. 设置属性语法
        attr('属性', '属性值')   //类似原生 setAttribute()

    该方法也可以获取 H5 自定义属性



3  数据缓存 data()  01
    data() 方法可以在指定的元素上存取数据，并不会修改 DOM 元素结构。一旦页面刷新，之前存放的数据都将被移除。 
    a. 附加数据语法
        data('name','value')    //向被选元素附加数据 

    b. 获取数据语法
        date('name')            //向被选元素获取数据   

    同时，还可以读取 HTML5 自定义属性  data-index ，得到的是数字型



4  购物车案例模块-全选分析   02
    a.全选思路：里面3个小的复选框按钮（j-checkbox）选中状态（checked）跟着全选按钮（checkall）走。
    b.为checked 是复选框的固有属性，此时我们需要利用prop()方法获取和设置该属性。
    c.全选按钮状态赋值给3小复选框就可以了。
    d.我们每次点击小的复选框按钮，就来判断：
    e.果小复选框被选中的个数等于3 就应该把全选按钮选上，否则全选按钮不选。
    f.hecked 选择器      :checked 查找被选中的表单元素。

